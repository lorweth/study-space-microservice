apply plugin: "war"

bootWar {
    mainClassName = "vn.vnedu.studyspace.group_store.GroupStoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
