apply plugin: "war"

bootWar {
    mainClassName = "vn.vnedu.studyspace.answer_store.AnswerStoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
