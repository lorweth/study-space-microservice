apply plugin: "war"

bootWar {
    mainClassName = "vn.vnedu.studyspace.exam_store.ExamStoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
